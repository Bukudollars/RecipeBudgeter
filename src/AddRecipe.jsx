import * as React from 'react';
import { Box } from '@mui/material';

function AddRecipe() {
    return (
        <Box>
            <h1>Add Recipe</h1>
        </Box>
    );
};
export default AddRecipe;